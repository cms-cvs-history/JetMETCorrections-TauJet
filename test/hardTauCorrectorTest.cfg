process test = {

    untracked PSet maxEvents = {untracked int32 input = -1}

    source = PoolSource {
        untracked vstring fileNames = {}
    }

replace PoolSource.fileNames = {
'/store/relval/2008/6/2/RelVal-RelValHiggsChargedTausM200-1212358378-IDEAL_V1-2nd/0000/E21389C9-AC30-DD11-8C4F-000423D6BA18.root'
}

    # Message Logger
    include "FWCore/MessageLogger/data/MessageLogger.cfi"

    # Magnetic Field
    include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

    # XML ideal geometry description
    include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"
    # Calo geometry service model
    include "Geometry/CaloEventSetup/data/CaloGeometry.cfi"
    # Calo topology service model
    include "Geometry/CaloEventSetup/data/CaloTopology.cfi"
    include "TrackingTools/TransientTrack/data/TransientTrackBuilder.cfi"


    module hardTauCorrectorTest = HardTauCorrectorTest {
		// Optional input for the HardTauAlgorithm listed in data/HardTauAlgorithm.cfg
		// copy-paste here any line which needs changing. Example:
		// untracked double EtCaloOverTrackMin = -0.9

		// for the test program: ProngSelection = "1prong","3prong","any" (any = 1 or 3 prong)
		string ProngSelection = "1prong"
		// for the test program: TauJet jet energy correction parameters
                InputTag src = iterativeCone5CaloJets
                string tagName = 'IterativeCone0.4_EtScheme_TowerEt0.5_E0.8_Jets871_2x1033PU_tau'
                int32 TauTriggerType = 1
    }

    # module execution
    path runEDAna = { hardTauCorrectorTest }
}
